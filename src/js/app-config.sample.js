// Copy this file to app-config.js and fill in the values.
// app-config.js is gitignored — never commit real credentials.
const APP_CONFIG = {
  backendUrl: '',   // e.g. https://hcapp.yourcompany.com
  apiKey:     '',   // X-API-Key sent with every backend request
};
